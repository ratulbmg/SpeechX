{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and the pill overlay",
  "windows": ["main", "pill"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
